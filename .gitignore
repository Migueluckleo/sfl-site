# ────────────── 📦 DEPENDENCIAS Y BUILDS ──────────────
node_modules/
dist/
build/

# ────────────── ⚙️ CONFIGURACIÓN Y TOOLS ──────────────
tailwind.config.js
postcss.config.js

# ────────────── 🚫 ARCHIVOS DE SISTEMA Y METADATA ──────────────
.DS_Store
Thumbs.db
*.log
.env
.vscode/
.idea/
*.bak
*.tmp

# ────────────── 💻 CONFIGS DE GIT Y CI/CD ──────────────
.github/            # (si no haces deploy desde este repo)
.gitlab/
.gitattributes
.gitattributes.lock

# ────────────── ⚠️ ARCHIVOS DEL PROYECTO A IGNORAR DEL DEPLOY ──────────────
# ⛔️ Estos se quedan en Git pero **no deben ir al servidor FTP**
package.json
package-lock.json

# ❗️EXCEPCIONES: NO ignoramos `src/input.css` porque lo usas para Tailwind
# ❗️NO ignoramos `src/output.css` porque es el CSS final del sitio
